<% root_dir = ENV['CSS'] %>

name: IDE
root: <%= root_dir %>

windows:
  - Be:
      root: <%= root_dir %>/backend-service
      # layout: even-horizontal
      layout: even-vertical
      panes:
        - nvim .
        - claude
  - Fe:
      root: <%= root_dir %>/web-app
      # layout: even-horizontal
      layout: even-vertical
      panes:
        - nvim .
        - claude
  - Pvs:
      root: <%= root_dir %>/portfolio-valuation-service
      # layout: even-horizontal
      layout: even-vertical
      panes:
        - nvim .
        - claude
